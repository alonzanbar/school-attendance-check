{
  "name": "school-attendance-check",
  "version": "1.0.0",
  "description": "Google Apps Script – school attendance check",
  "scripts": {
    "login": "clasp login",
    "create": "clasp create --type webapp --title \"school-attendance-check\"",
    "clone": "clasp clone",
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "deploy": "clasp deploy --description \"Deploy from repo\"",
    "deploy:prod": "clasp deploy --description \"Production\"",
    "logs": "clasp logs"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
